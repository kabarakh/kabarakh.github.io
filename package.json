{
  "name": "gridsome-kabarakh",
  "private": true,
  "scripts": {
    "build": "NODE_OPTIONS=--openssl-legacy-provider gridsome build",
    "develop": "NODE_OPTIONS=--openssl-legacy-provider gridsome develop",
    "explore": "NODE_OPTIONS=--openssl-legacy-provider gridsome explore",
    "deploy": "NODE_OPTIONS=--openssl-legacy-provider gridsome build && gh-pages -d dist"
  },
  "dependencies": {
    "gridsome": "^0.7.0",
    "bootstrap": "^5.2.0"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "sass": "^1.54.8",
    "sass-loader": "^10.0.2"
  }
}
