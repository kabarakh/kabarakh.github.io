{
  "name": "iles-app",
  "version": "0.0.0",
  "scripts": {
    "dev": "iles dev --open",
    "build": "iles build",
    "preview": "iles preview --open --port 5050",
    "typecheck": "vue-tsc --noEmit",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "engines": {
    "node": "^14.18 || >= 16.0.0"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "iles": "^0.8.0",
    "sass": "^1.54.8",
    "typescript": "^4.5",
    "vue-tsc": "^0.38"
  },
  "dependencies": {
    "bootstrap": "^5.2.0"
  }
}
